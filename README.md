#for postgres database
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "postgres",
        "USER": "postgres",
        "PASSWORD": "reeja123",
        "HOST": "127.0.0.1",
        "PORT": "5432",
    }
}