[project]
name = "smart-waste-managment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "reeja shakya", email = "reejashakya099@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
smart-waste-managment = "smart_waste_managment:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
